# This file contains the environment variables needed to run the application.
# Copy this file to .env and fill in the values for your local setup.
# IMPORTANT: Do not commit the .env file to version control.

# PostgreSQL Settings
POSTGRES_DB=ecommerce
POSTGRES_USER=admin
POSTGRES_PASSWORD=mysecretpassword

# JWT Settings
# Use a long, random string for production
JWT_SECRET=default-super-secret-key-for-dev-use-a-real-one-in-prod
# Expiration time in seconds (e.g., 86400 for 24 hours)
JWT_EXPIRATION=86400
